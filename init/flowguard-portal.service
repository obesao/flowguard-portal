[Unit]
Description=Portal do Provedor — busybox httpd (dashboard + CGI)
After=network.target flowguard.service clientguard.service

[Service]
Type=simple
ExecStart=/usr/bin/busybox httpd -f -v -p 18080 -h /root/site
Restart=on-failure
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
